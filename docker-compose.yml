services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "5002:5000"
    volumes:
      - .:/app
    environment:
      FLASK_ENV: development
      PORT: 5000
    restart: unless-stopped
      