services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    volumes:
      - .:/app
    environment:
      FLASK_ENV: development
      PORT: 5000
    restart: unless-stopped
      
